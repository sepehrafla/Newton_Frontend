export interface CryptoMeta {
  prefix: string;
  name: string;
  iconUrl: string;
}

export interface CryptoData {
  name: string;
  symbol: string;
  change: string;
  price: string;
  sellPrice: string;
  buyPrice: string;
  iconUrl: string;
} 